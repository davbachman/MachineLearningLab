# Archived notebooks

`7Overfitting.ipynb` is preserved for reference only. Its content has been combined into `../5PolynomialRegression.ipynb`, titled **Homework 5: Polynomial Regression and Overfitting**. Use that self-contained notebook with the combined app assignment.

The archived notebook retains its old numbering, scikit-learn code, and known issues (the reversed split and undefined `PolynomialRegression` in the metrics loop); it is not the student assignment. Current numbering is listed in `../README.md`.
